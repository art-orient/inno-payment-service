spring:
  application:
    name: payment-service

  mongodb:
    uri: mongodb://localhost:27017/payments_db

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml